import React from "react";
import { CartContainer } from "../components";

const Cart = () => {
  return (
    <div className="container p-4">
      <CartContainer />
    </div>
  );
};

export default Cart;
